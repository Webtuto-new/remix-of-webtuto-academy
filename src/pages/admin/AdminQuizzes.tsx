import AdminQuizCenter from "./AdminQuizCenter";

// Master Quiz Center is now the canonical admin quiz page.
// We keep the legacy file name as a re-export so the route in App.tsx still works.
export default AdminQuizCenter;